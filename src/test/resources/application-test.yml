testcontainers:
  common:
    postgres:
      enable: false
    keycloak:
      enable: true
    mongodb:
      enable: true

spring:
  data:
    migrations:
      weaviate:
        enabled: false
    mongodb:
      uri: ${testcontainers.common.mongodb.url}
      database: test